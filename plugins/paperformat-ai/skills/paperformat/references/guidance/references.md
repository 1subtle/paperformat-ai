# Protect references

Check supported heading, paragraph, font, size, alignment, spacing, indentation,
and direct-format consistency rules. Keep reference text and citation fields
immutable.

Do not convert bibliographic style, rewrite entries, renumber citations, or
modify EndNote/Zotero fields without a separate supported toolchain. Report
semantic citation-format differences as unsupported.
