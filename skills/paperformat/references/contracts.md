# Contract locations

Use the authoritative versioned schemas in the repository-level `schemas/`
directory. Do not copy or edit schemas inside Agent adapters.

Primary Agent contracts:

- `agent-plan-proposal.schema.json`;
- `repair-plan.schema.json`;
- `apply-manifest.schema.json`;
- `agent-visual-review-submission.schema.json`;
- `validated-visual-review.schema.json`;
- `validation-report.schema.json`;
- `export-manifest.schema.json`;
- `workflow-manifest.schema.json`.

Skill packaging contract:

- `template-asset-registry.schema.json` for
  `skills/paperformat/assets/templates/index.json`.
